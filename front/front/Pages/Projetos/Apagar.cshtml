@page
@model front.Pages.Projetos.ApagarModel
@{
    ViewData["Title"] = "Apagar Projeto";
}

<div class="fixed inset-0 w-full h-full bg-[url('/images/background.png')] bg-cover bg-center -z-10"></div>

<div class="relative min-h-screen w-full p-10 z-10 flex items-center justify-center">
    <div class="bg-white bg-opacity-95 p-10 rounded-2xl shadow-xl max-w-xl w-full">
        <h2 class="text-2xl font-bold text-red-700 mb-6 text-center">⚠️ Apagar Projeto</h2>

        <p class="text-gray-800 text-lg mb-4">
            Tens a certeza que queres apagar o projeto
            <span class="font-semibold text-red-800">"@Model.Projeto.Nome"</span>
            do cliente <span class="font-semibold">@Model.Projeto.NomeCliente</span>?
        </p>

        <p class="text-gray-600 mb-8">
            Esta ação não pode ser desfeita. Todos os dados associados serão permanentemente removidos.
        </p>

        <form method="post" class="flex justify-between">
            <a asp-page="/Projetos/Index" class="bg-gray-300 hover:bg-gray-400 text-gray-800 px-5 py-2 rounded-lg transition">
                Cancelar
            </a>
            <button type="submit" class="bg-red-600 hover:bg-red-700 text-white px-5 py-2 rounded-lg transition">
                Apagar
            </button>
        </form>
    </div>
</div>
